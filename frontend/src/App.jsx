import AppRoutes from './routes/AppRoutes';

/**
 * Root application component.
 * Router provider lives in main.jsx; route table lives in routes/AppRoutes.
 */
export default function App() {
  return <AppRoutes />;
}
