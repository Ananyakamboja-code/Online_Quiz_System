import AppLayout from '../../components/AppLayout';

/**
 * Admin module entry page (placeholder).
 * Owned by the Admin developer. Build admin features under src/pages/admin/.
 */
export default function AdminPage() {
  return (
    <AppLayout role="admin">
      <h1 className="h3">Admin</h1>
      <p className="text-muted">
        Admin module placeholder. Admin features will be built here.
      </p>
    </AppLayout>
  );
}
