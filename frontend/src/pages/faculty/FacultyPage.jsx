import AppLayout from '../../components/AppLayout';

/**
 * Faculty module entry page (placeholder).
 * Owned by the Faculty developer. Build faculty features under src/pages/faculty/.
 */
export default function FacultyPage() {
  return (
    <AppLayout role="faculty">
      <h1 className="h3">Faculty</h1>
      <p className="text-muted">
        Faculty module placeholder. Faculty features will be built here.
      </p>
    </AppLayout>
  );
}
